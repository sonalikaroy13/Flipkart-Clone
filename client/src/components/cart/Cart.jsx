import {makeStyles, Box} from '@material-ui/core';

const useStyle = makeStyles({
    
})

const Cart = () => {
    const classes = useStyle();
    return (
        <Box>
            Hi from cart
        </Box>
    )

}

export default Cart;